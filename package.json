{
  "name": "threejs-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.17.3",
    "dat.gui": "^0.7.9",
    "three": "^0.176.0"
  },
  "homepage": ".",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:docs": "rm -rf docs && npm run build && cp -r dist docs && echo \"boxer.lumus.games\" > docs/CNAME && touch docs/.nojekyll && git add docs docs/.nojekyll docs/CNAME && git commit -m \"Deploy via docs folder: güncel build\" || echo \"No changes to commit\" && git push origin main"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  }
}